services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
    command: sh -c "uvicorn src.api:app --host 0.0.0.0 --port 8000 --reload & streamlit run src/app.py --server.address=0.0.0.0 --server.port=8501"