[project]
name = "loadforecasting"
version = "0.1.0"
description = "Load Foreceastin Data Analytics app"
requires-python = ">=3.12"
dependencies = [
    "joblib>=1.5.3",
    "numpy>=2.4.2",
    "pandas>=3.0.0",
]
